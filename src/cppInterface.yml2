include ./Interface.yml2

textstylesheet {
    template "interface"
        ||
        #ifndef CPP_«@name»_H
        #define CPP_«@name»_H

        #include <vector>
        #include <string>

        class «@name» {
        public:
            `` apply "fn"
        };

        #endif
        ||

    template "fn"
        ||
        virtual «@ret» «@name» = 0;  // todo params
        ||
}